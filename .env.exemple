# Copier ce fichier en `.env` et renseigner les valeurs réelles.
# Ne jamais committer le fichier `.env` (secrets).

DB_NAME=mdd
DB_USER=
DB_PASSWORD=
DB_ROOT_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Secret de signature des JWT (chaîne longue et aléatoire)
TOKEN_SECRET=
